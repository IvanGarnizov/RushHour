@using RushHour.App.Models.ViewModels
@model IEnumerable<AppointmentViewModel>
    
<h2>Appointments</h2>
@if (Model.Count() == 0)
{
    <p>No appointments</p>
}
else
{
    @Html.DisplayFor(m => m)
}
<hr />
@Html.ActionLink("Create Appointment", "Create", "Appointment", null, new { @class = "btn btn-success" })